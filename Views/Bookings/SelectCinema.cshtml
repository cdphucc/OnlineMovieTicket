@model IEnumerable<OnlineMovieTicket.Models.Cinema>
<h2>Chọn rạp</h2>
<ul>
    @foreach (var cinema in Model)
    {
        <li>
            <a href="@Url.Action("SelectMovie", "Bookings", new { cinemaId = cinema.Id })">@cinema.Name</a>
        </li>
    }
</ul>